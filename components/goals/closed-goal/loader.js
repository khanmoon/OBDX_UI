define(["module", "text!./closed-goal.html", "./closed-goal", "text!./closed-goal.json"], function (module, template, viewModel) {
    "use strict";
    return {
      viewModel: viewModel,
      template: template
    };
  });