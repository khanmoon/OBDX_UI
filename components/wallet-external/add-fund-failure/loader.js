define(["module", "text!./add-fund-failure.html", "./add-fund-failure", "text!./add-fund-failure.json"], function (module, template, viewModel) {
    "use strict";
    return {
      viewModel: viewModel,
      template: template
    };
  });